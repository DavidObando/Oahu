# cs2gs ilverify allow-unsafe marker (gsharp issue #1933 / #1985).
#
# These extension methods use `stackalloc` (Span<byte> over a localloc buffer).
# `localloc` is inherently unverifiable IL, and the C# compiler emits the same
# instruction. cs2gs lowers the extension container to this package host type.
Oahu.Decrypt.Mpeg4.Util.<Program>
